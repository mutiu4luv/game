import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
// import { LoaderComponent } from 'src/app/component/loader/loader.component';
// import { SponsorComponent } from 'src/app/pages/sponsor/sponsor.component';
// import { FooterComponent } from 'src/app/component/footer/footer.component';
import { FormGroupName, NgModel } from '@angular/forms';
// const component = [LoaderComponent, SponsorComponent, FooterComponent];
// const module = [CommonModule, NgModel, FormGroupName];

@NgModule({
  // declarations: [...component],
  // imports: [...module],
  // exports: [...component, ...module],
})
export class SharedmoduleModule {}
